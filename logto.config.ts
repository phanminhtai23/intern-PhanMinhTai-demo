export const logtoConfig = {
    endpoint: 'https://your-logto-endpoint', // URL của Logto server
    appId: 'your-app-id',                   // App ID từ Logto
    appSecret: 'your-app-secret',           // App Secret từ Logto
};